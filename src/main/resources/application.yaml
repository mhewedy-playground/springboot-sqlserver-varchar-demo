spring:
  jpa:
    database: sql_server
  datasource:
    username: sa
    password: yourStrong(!)Password
    url: jdbc:sqlserver://localhost:1433;DatabaseName=mydb;sendStringParametersAsUnicode=false
    initialization-mode: always
logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type: TRACE
